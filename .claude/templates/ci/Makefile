# Template Makefile — copy into repo root once the core language is chosen
# and fill in the language-specific commands below each target.

.PHONY: build test lint fmt clean graph

build:
	# e.g. cargo build / go build ./...
	@echo "TODO: wire build command"

test:
	# e.g. cargo test / go test ./...
	@echo "TODO: wire test command"

lint:
	# e.g. cargo clippy / golangci-lint run
	@echo "TODO: wire lint command"

fmt:
	# e.g. cargo fmt / gofmt -w .
	@echo "TODO: wire format command"

clean:
	# e.g. cargo clean / go clean
	@echo "TODO: wire clean command"

graph:
	python -m code_review_graph build
