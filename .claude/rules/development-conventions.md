# Development Conventions

**Status:** stub — not yet defined. Needs a `/superpowers:brainstorm` pass.

## Scope

General day-to-day engineering conventions for this repo: coding style,
testing expectations, commit hygiene, and anything else that applies
across the whole codebase regardless of language.

## Open questions to resolve via brainstorming

- Core language(s) for TTUI itself are not yet chosen (Rust vs Go is the
  live question — see root `CLAUDE.md`). Conventions here likely can't
  be finalized until that's settled.
- What testing discipline applies (unit/integration split, coverage
  expectations, TDD enforcement beyond what `superpowers`' own
  `test-driven-development` skill already provides)?
- Commit granularity and message conventions.
